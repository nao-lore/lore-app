# Lore - Environment Variables
# Copy this file to .env and fill in your values.

# No custom environment variables are currently used.
# All API keys are configured in the app's Settings UI.
# The only env flags used are Vite built-ins (e.g. import.meta.env.DEV).
